# 详细设计说明书

**项目名称**：course-tools zsgl 模块
**作者**：李荣起
**日期**：2026-09-15
**版本**：1.26.915.699
**生成范围**：全部 7 套
**文档语言**：中文

## 变更日志

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| 1.26.915.699 | 2026-09-15 | 李荣起 | 初始版本 |

---

> 行号引用基于当前源码，供快速定位；平台前端改版不影响本文结构描述。

## 一、基础设施（utils/utils.ts）

### 1.1 HTTP 拦截 hookHttpRequest / hookAndModifyHttpResponse

**算法流程**（utils.ts:50-174）：

1. 模块级单例 `hooksInitialized` 保证 `XMLHttpRequest.prototype.open` 只被替换一次。
2. 替换后的 `open(method, url)`：
   - 遍历 `httpHooks` Map，若 `url.includes(urlMatch)`，则在当前 XHR 上追加 `readystatechange` 监听；
   - `readyState === 4 && status === 200` 时解析 `responseText`（`{` 开头按 JSON，否则原样字符串），依次调用注册的 `callback(response, context, url)`；
   - 遍历 `modifyHooks`，命中后在 readystatechange 中改写实例属性：`Object.defineProperty(xhr, 'responseText'/'response', { get: () => modifiedText })`，实现响应篡改；
   - 最终调用原始 `originalOpen.apply(this, arguments)`，不改变真实请求行为。
3. `hookHttpRequest(urlMatch, callback, context)` 仅向 Map 注册回调（异步立即 resolve）。

**注意点**：每次 `hookHttpRequest` 调用都会追加新的监听器且无去重/注销机制；同一请求 URL 匹配 N 个钩子时回调执行 N×M 次（长会话重复注册风险，见风险清单）。

### 1.2 TimerManager（utils.ts:286-327）

以 `Map<string, timeoutId|intervalId>` 按命名 key 管理 setTimeout/setInterval；`setTimeout/setInterval` 前先清理同名 key（幂等）；`clearAll()` 清空全部。所有 zsgl 业务类持有私有实例，`Stop()` 时统一释放。

### 1.3 事件阻止（utils.ts:226-281）

- `setupEventPrevention(target)`：对 window/document/元素以捕获阶段注册 `blur/resize/visibilitychange/fullscreenchange/focus/webkitfullscreenchange/mouseout/mouseleave/focusout/focusin/pagehide/pageshow/beforeunload/freeze/resume` 共 15 类事件的处理器，处理器内执行 `stopImmediatePropagation + stopPropagation`，切断平台"切屏检测"监听。
- `setupVideoEventPrevention(video)`：对 `seeked/seeking/ratechange/volumechange` 做同样阻止，掩盖倍速/静音篡改痕迹。
- 已知副作用：自身监听（如 keep-alive 的 visibilitychange、ratechange 防重置）也注册在同一目标上，捕获阶段顺序决定其是否被阻断。

### 1.4 DOM 工具

- `findElementByText(tagName, text)`：全量 `querySelectorAll(tagName)` 后按 `textContent.includes(text)` 线性查找首个命中元素（course 任务卡、播放入口依赖此函数）。
- `waitForElement(selector, {timeout=10000, interval=500})`：轮询查找，超时返回 null。
- `createStorageHandler(key)`：storage 事件处理器；`event.key === key` 且新值 `status === "finished"` 时移除自身并 `window.location.reload()`（学习地图任务完成后的回环入口）。
- `sendApiRequest(method,url,params,body,headers,timeout=30s)`：AbortController 超时控制的 fetch 封装，`credentials:"include"`，统一返回 `ApiResponse{success,data,error,status}`。

## 二、平台路由（platform.ts）

**ZsglPlatform.CreateMooc()**（platform.ts:28-65）：

```
SetNamespace("zsgl")                     // 配置命名空间，必须最先执行
hash 含 /home/courseDetail/  → new ZsglCourse()
hash 含 /home/studyDetail    → new ZsglStudyMap()
hash 含 /home/examDetail     → new ZsglExam()
否则 → null（扩展在该页面不工作）
```

> 源码中被注释的 `daily_points_mode` 分支：每日积分模式当前不作为独立页面路由，`ZsglDailyPoints` 由悬浮面板经消息驱动（dailyPointsPanel 属内核组件）。

## 三、课程任务集（course.ts）

### 3.1 ZsglCourse.Init()（course.ts:43-87）

1. 注册 `courseTaskComplete` 事件 → 触发 `notifyStudyMapCourseComplete()`。
2. `setupCloseCourseListener()`：注册 storage 监听 + 3s 轮询，双通道检测 `zsgl_close_course_{courseId}` 键值为 `{status:"close"}`；命中后移除该键、`Stop()` 全部任务、`window.close()`，1s 后仍存活则 `alert('学习积分已达上限，请关闭此页面')`。
3. `hookCourseDetailRequests()`（course.ts:93-163）：
   - 拦截 `queryCourseDetail.do`，读取 `body.courseId`、`body.courseFileArr`；
   - `courseFileArr.every(hasLearned==="1")` 或数组为空 → `callEvent("courseTaskComplete")`；
   - 否则映射为 `CourseDetailItem[]`（补 courseId、jobIndex=index、playTime/learnedDuration）并 `filter(hasLearned==="0")` 存入 `courseDetailData`。
4. `load` 事件或页面已就绪时：在 `#watermarkFrame`（兜底 body）顶部插入占位 div（控制栏挂载点，当前 `ZsglCourseControlBar` 创建被注释），调用 `OperateCard()`，并以 `first` 标志保证 Promise 仅 resolve 一次。

### 3.2 OperateCard / processCourseData / buildTasks

- **OperateCard**（course.ts:294-318）：数据为空时启动 `waitForData` 轮询（1s 间隔，20 次上限）等待拦截回调填充；超时则 `courseTaskCompleteFc()` 放行。数据就绪 → `processCourseData()`。
- **processCourseData**（course.ts:321-343）：以首个任务的 `fileName` 为锚点，`checkTaskDiv` 轮询（500ms 间隔，10 次上限）用 `findElementByText("span", fileName)` 找任务卡；命中 → `buildTasks()`、`taskIndex=0`、`callEvent("reload")`；超限 → 视为完成。
- **buildTasks**（course.ts:346-370）：按序遍历 `courseDetailData` → `TaskFactory.CreateCourseTask(value)`；返回 null（不支持的 cwType）则告警跳过；成功则赋 `jobIndex`、注册 `complete → callEvent("taskComplete", index, task)`、`await task.Init()` 入列。

### 3.3 任务调度与完成上报

- `Next()`（course.ts:277-287）：`taskList[taskIndex++]`；越界时 `courseTaskCompleteFc()`（发布事件 + 上报完成）。
- `notifyStudyMapCourseComplete`（course.ts:172-215）：遍历 localStorage，找到 `zsgl_task_{currentCourseId}` 键 → 改写为 `{status:"finished", expire}` → `window.close()`；未找到则告警。（`zsgl_daily_task_*` 前缀在同一循环中被匹配，但其 finished 改写仅针对课程键。）

### 3.4 翻页（保留能力）

`afterPage()/nextPage(num)`（course.ts:373-415）：基于 `.ncells/.currents/.orange01/.lock` 选择器的章节翻页逻辑；`nextPage` 对 `.lock` 存在的情况每 5s 重试，`num > 5` 时报"被锁卡住了"错误。当前主流程未调用（多任务卡片单页场景），为历史/兜底能力。

## 四、任务基类与工厂（task.ts / factory.ts）

### 4.1 ZsglTask（task.ts:21-70）

- 构造时依据 `taskinfo.hasLearned` 判定 `done`（exam 无该字段则 false）。
- `callEvent("taskComplete")` 拦截：先置 `done = true` 再发布事件。
- 默认 `Init/Submit/Stop` 空实现，`Start()` 抽象。

### 4.2 TaskFactory（factory.ts:27-69）

- `CreateCourseTask(taskinfo)`：cwType 不在 `SUPPORTED_COURSE_TYPES` → null；`scorm → ZsglAudio`、`video → ZsglVideo`、`knowledge → ZsglKnowledge`；`document/audio/URL` 打日志后返回 null。
- `CreateQuestionTask(taskinfo)`：无条件 `new ZsglQuestionTask(document, taskinfo)`。

## 五、视频任务（video.ts）

### 5.1 生命周期

```
Init()  : findElementByText('span', fileName) 找任务卡 + querySelector('video#course-video_html5_api')
          两者齐备 resolve，否则 reject(TASK_NOT_FOUND)
Start() : setupVideoEndHandler() → setupEventPrevention(window/document/video)
          → startKeepAlive() → taskDiv.addEventListener('click', once 处理器)
          → taskDiv.click()（模拟用户点击打开播放器）→ resolve
```

### 5.2 播放器初始化 initPlayer（video.ts:128-149）

1. 按配置 `video_mute` 设音量；`video.muted=false`（后续 clickPlayButton 内再临时静音绕过自动播放策略）。
2. `watchPlaybackRate()`：监听 `ratechange`，若 `playbackRate !== 配置值` 立即改回（对抗平台重置）。
3. `waitForVideoSourceAndPlay()`：每 1s 检查 `src/currentSrc/source/readyState`，最多 20 次；就绪后设倍速、`currentTime=0`，`auto` 开启时 `clickPlayButton()`；同时挂 `loadedmetadata/canplay`（once）兜底触发。
4. 500ms 后再次 `currentTime=0` 并设倍速（修复平台续播导致的时长不足）。

### 5.3 自动恢复与保活

- `setupVideoAutoResume()`：5s 轮询"auto 且 paused → clickPlayButton"；`pause` 事件 100ms 后恢复；`play` 事件 100ms 后重设倍速/静音。
- `clickPlayButton()`（video.ts:212-235）：先 `muted=true` 绕过自动播放策略 → 点击 `.vjs-big-play-button`（Video.js 大播放键）或直接 `video.play()`，失败回退点击按钮 → 恢复 `setMute()` 与 `setPlaybackRate()`。
- `startKeepAlive()`：播放 base64 内联 WAV 静音音频（loop，volume 0.001）维持浏览器媒体会话；`visibilitychange` 时若隐藏且暂停则重新 play。
- `setupVideoEndHandler()`：`ended`（once）→ 清理恢复轮询与保活 → `notifyVideoTaskComplete()` → `callEvent("complete")`。
- `notifyVideoTaskComplete()`：`localStorage['zsgl_video_complete_{courseId}_{jobIndex}'] = {status:"finished", courseId, taskId, timestamp}`（供每日积分模式监听，普通课程流程不依赖）。

## 六、SCORM 任务（scorm.ts，类名 ZsglAudio）

与视频任务共用播放逻辑（initPlayer/waitForVideoSourceAndPlay/clickPlayButton/watchPlaybackRate/keepAlive 基本同构），差异在启动链路：

1. **Start**：`taskDiv.click()` → 事件阻止 → `outerInit` 轮询（1s）找 `.MuiButton-root`；scorm 类型时以 `{once:true}` 挂 click 处理器并 `startButton.click()`；非 scorm 直接 resolve。
2. **findvideoinit**：`videoSearch` 轮询（1s，≤10 次）调用 `deepFindVideo(document)`。
3. **deepFindVideo**（scorm.ts:339-404，递归）：
   - 当前文档中找第一个"有源"（`src || currentSrc || source.src`）的 `<video>`；
   - 遍历 `frame/iframe`：`frame.id === 'course_frame_id'` 时优先在其中找 video；否则递归进入 `contentDocument`（跨域抛错被捕获并告警）。
4. 播放按钮为 xgplayer（西瓜播放器）的 `xg-start.xgplayer-start`（与课程页 Video.js 不同）。
5. `ended` 处理额外执行 `exitBtn?.click()`（`span.exit`）退出课件页，并同样写视频完成 localStorage。

## 七、知识点任务（knowledge.ts）

**Start 流程**（knowledge.ts:44-57，四步串行）：

1. `taskDiv.click()` 打开学习弹层。
2. `waitForStartButton()`：500ms 轮询 `button.MuiButton-root`，文本含"立即学习/开始学习/学习"即点击；≤10 次超限 reject。
3. `waitForIframeLoad()`：轮询首个带 `src` 的 `iframe`，同时记录 `span.exit` 退出按钮；≤10 次超限 reject。
4. `waitAndExit()`：`remaining = playTime*1000 - learnedDuration + 3000`；≤0 立即 `exitBtn.click()` + complete；否则 `timerManager.setTimeout` 等待后同样处理。

> `playTime/learnedDuration` 来源于 `queryCourseDetail.do` 响应字段（单位：秒），由 course.ts 映射进 `TaskInfo`。

## 八、学习地图（studyMap.ts）

### 8.1 Init（studyMap.ts:31-63）

依次 `hookStudymapGateRequests()` → `hookStudymapGateTaskRequests()` → 有关卡数据则 `setupCurrentLevelButton()`，否则 `Done()`；注册 ChromeServerMessage("zsgl-tools") 监听 `zsgl_task_{resourceId}` 消息触发 reload（扩展后台通知通道，与 storage 通道并存）；`load` 事件 once 后重新 `Init()`（SPA 页面刷新复挂载）。

### 8.2 关卡拦截

- `hookStudymapGateRequests`：读 `body` 关卡数组映射 `StudyMapData`；`skip_elective` 开启 → 找 `status !== 3`，否则找 `finishTaskNum !== taskNum` 的首个关卡存 `studyMapData`。
- `hookStudymapGateTaskRequests`：3s 超时兜底 resolve；回调中校验请求参数 `studymapGateId` 与当前关卡一致（不一致继续等待下一个请求）；取 `taskList.find(status!==1 && resourceType!==2)` 存 `gateTaskData`；无可做任务时区分"全部完成（关卡置 3）/仅剩考试（关卡置 1）"并 `findNextLevelAndStart()`。

### 8.3 关卡点击与任务启动

- `setupCurrentLevelButton`：500ms 轮询找文本含 `第 {gateNameIndex+1} 关` 的 `li`，挂 click（再次 hook 任务接口，auto 开启则 Start），并主动 `click()`。
- `Start`（studyMap.ts:321-356）：轮询 `li.MuiListItem-root` 中 `<p>` 文本含 `gateTaskData.taskName` 的元素 → 点击 → 写 `zsgl_task_{resourceId} = {status:"started", expire: now+10h}` → `createStorageHandler` 监听 finished → resolve。
- `findNextLevelAndStart`：重新从 `studyMapDataList` 按规则选关 → 按钮点击 → 重新 hook 任务接口 → auto 时 Start；无未完成关卡 → `studyMapData = null`（Done）。

## 九、考试模块（exam.ts）

### 9.1 数据获取与解密

- `decryptData`（exam.ts:356-375）：`crypto-js AES.decrypt(cipher, Utf8(SECRET_KEY), {iv: Utf8(IV), CBC, Pkcs7})`；失败返回 null 并记录错误。密钥/IV 见 constants.ts `AES_CONFIG`。
- `hookQuestionDetailRequests`：拦截 `queryQuestionDetail.do` → 解密 → `JSON.parse(JSON.parse(decrypted).body)`（双重解析）→ `adaptQuestionData` 适配（questionType 校验 S/M/T，非法默认 "S"；选项 isCorrect 缺省 "N"）→ 判定 `every(存在 isCorrect==="Y")`：不满足 → `startAutoAnswering()`；满足 → 仅展示提示。
- `hookQueryNewExamPaper`：拦截 `queryNewExamPaper.do` → 解密 → 提取 `examId/attemptId/testNo/questionIdList`，四者齐备 resolve。

### 9.2 自动答题编排 startAutoAnswering（exam.ts:436-503）

```
缺 examId/attemptId/testNo → await hookQueryNewExamPaper()
单选题+S/T 判断题 → crackSingleChoiceQuestions()（SingleChoiceAnswerStrategy.crackSingleChoiceBatch）
多选题 M → startMultipleChoiceAnswering()
  → new MultipleChoiceAnswerStrategy(examId, attemptId, testNo)
  → crackMultipleChoiceBatch(questions)
  → updateQuestionListWithMultipleChoiceAnswers(): finalAnswer 命中选项 isCorrect="Y"，其余 "N"
全程记录耗时（单选/多选/总耗时）
```

### 9.3 UI 编排

- `setupReturnButton/setupNavigationButtons`：500ms 轮询查找"返回/交卷/下一题"按钮；`nextButton.click` 追加 `answerMessage(questionList)`（延迟 1s，对 `.MuiPaper-root` 全量元素按题干匹配并 Info 输出正确答案集）。
- `OperateCard`：为每题 `TaskFactory.CreateQuestionTask` 建任务（complete 事件 → `questionTaskComplete` → Launcher 以 0 延时 Submit 并推进），完成后 `callEvent("examReload")`；`Next()` 越界 → `examTaskComplete` → Launcher `window.close()`。
- `ZsglQuestionTask.Init`：轮询锁定"上一题/下一题/交卷"按钮；`jobIndex === 0` 时给"下一题"挂一次性 complete 监听（首题点击视为开始答题）。

### 9.4 单选排除法 SingleChoiceAnswerStrategy（exam-answer-strategy.ts）

```
initializeAnswerStates(questions): 仅 questionType==="S" 入列
  state = {questionId, triedOptions[], confirmedAnswer:null, options, originalQuestion}
循环 round < 4 且有 pending:
  1. buildBatchAnswerData: 每题取 getNextOptionToTry()（首个未尝试选项），
     组装 {attemptId, examId, testNo, answerList:[optionId], questionId, ...}
  2. submitQuestionAnswer(batch)         → code 非 0 终止
  3. queryQuestionAnswer(examId,attemptId) → 构建 questionId→result Map
  4. result.isCorrect==="Y" → confirmedAnswer=当前选项
     否则 markOptionAsWrong(当前选项)；仍有未尝试选项 → 留待下一轮
统计 successCount/failedCount/totalRounds/totalApiCalls
```

### 9.5 多选两阶段 MultipleChoiceAnswerStrategy（utils/exam-answer-strategy.ts）

- **第一阶段 testIndividualOptions**：对每题按选项索引逐个单独提交（answerList 仅含该选项），据 `isCorrect` 归入 `correctOptions` 或 `wrongOptions`；通过 `progressCallback` 上报进度（testing 阶段）。
- **第二阶段 verifyCombinations**：将候选正确选项组合提交验证，确定 `finalAnswer` 并置 `isCompleted`。
- 结果经 `updateQuestionListWithMultipleChoiceAnswers` 回写题目数据，使 UI 提示路径复用同一答案来源。

## 十、每日积分（dailyPoints.ts）

### 10.1 状态模型

```
DailyPointsState {
  learning/contribution/interaction: {type, current, limit(100/300/100), target},
  lastUpdate, isRunning, taskDelay(默认 8000ms)
}
持久化: localStorage['zsgl_daily_points_state']，按天（0 点）重置
```

### 10.2 消息协议（window.postMessage，同源）

| 方向 | type | 载荷 | 处理 |
|------|------|------|------|
| 面板 → 任务 | `CONFIRM_START_TASK` | learningLimit/contributionLimit/interactionLimit/taskDelay(秒)/knowledgeLink | 更新 targets/taskDelay/knowledgePageUrl → `executeTaskAfterConfirm()` |
| 面板 → 任务 | `REFRESH_POINTS` | - | `handleRefreshPoints()`：重取 sid → 拉积分详情 → 广播 POINTS_UPDATED |
| 面板 → 任务 | `STOP_DAILY_POINTS` | - | `stopTask()` |
| 任务 → 面板 | `POINTS_UPDATED` | 三类积分状态 | 刷新面板 |
| 任务 → 面板 | `TASK_STOPPED` | - | 面板复位 |

### 10.3 主控流程

- `executeTaskAfterConfirm`：提取 pageId（knowledgePageUrl 或当前 URL 的 `/knowledgePage/([A-Fa-f0-9]+)`）→ 提取 sid（localStorage `sessionInfo.sid`，兜底遍历含 "session"/"sid" 的键，再兜底 16 进制长串）→ 拉积分详情 → 全满则停止 → `switchToNextTask()`。
- `switchToNextTask`（dailyPoints.ts:681-721）：`isSwitching` 防重入；循环 `getNextTask()`（优先级 knowledgeShare → knowledgeRead → course，对应积分未满且前置条件满足）→ 执行对应任务 → 无任务可做或 isTaskStopped 退出。
- **知识类任务**（executeKnowledgeReadTask / executeKnowledgeShareTask，逻辑同构）：`pointsGap = target - current`（target=0 视为已满）；循环调 API，每次成功 `accumulated += 0.6`、`callCount++`；每 5 次 `fetchPointsDetail` 校准并重算 gap；间隔 `getRandomDelay()` = taskDelay ± 5s 随机；终止条件：isTaskStopped / gap≤0 / needSwitchTask（连续 2 次积分详情 JSON 无变化）。
- **课程任务**（executeCourseTask）：分页拉 `courselist.do`（每页 30，sortType=2）→ `findUnfinishedCourseFromData`（`isCompleted === 0 || "0"`）→ `setTaskStarted(courseId)` 写 `zsgl_daily_task_{id}`（30min 过期）→ `openCourseInNewWindow`（`#/home/courseDetail/{id}`）→ `waitForTaskComplete`（storage 事件 + 3s 轮询双通道，30min 超时）→ 完成则继续下一课程，超时退出循环。
- **视频完成联动**：`setupVideoCompleteListener` 监听 `zsgl_video_complete_*`（storage + 5s 轮询双通道）→ `handleVideoTaskComplete(courseId)`：拉积分详情 → 学习积分达 target → `notifyCloseCourse`（写 `zsgl_close_course_{id}`）→ `stopTask()`。
- **stopTask**：置位停止标志、clearAll 定时器、abort 进行中请求、移除 message/storage 监听、保存状态、广播 TASK_STOPPED。

## 十一、考试 API 签名（utils/exam-utils.ts）

### 11.1 getSid（exam-utils.ts:89-106）

内存 `memorySid` → cookie `sessionInfo`（JSON 解析取 sid）→ 空串。

### 11.2 generateHeaderMap（exam-utils.ts:245-273）

```
nonce   = md5(Date.now() + Math.random 的 36 进制串)
timestamp = Date.now()
signKey = getSignKey(): 测试环境（host 含 api-stg/test/stg/localhost/zsgltest/127.0.0.1/
          .w-stg1.zhi-niao.com/custom-stg1.zhi-niao.com）→ appId
          正式 → 'ERZF2pSLHBxyZE6t'
signStr = signKey + appDevicePlatform(99) + nonce + timestamp
          + 排序后的参数拼接（key + value；数组逐元素 JSON 串拼接；null/undefined 记空串）
sign    = md5(signStr)
headerMap = JSON.stringify({appId, nonce, sign, timestamp, appDevicePlatform:"99"})
```

### 11.3 sendExamApiRequest（exam-utils.ts:311-450）

`GET/POST /learn/app/clientapi{apiPath}?...&os=99[&sid]`，请求头含 `sid`、`headerMap`、`appDevicePlatform:99`、`Language:zh` 等；响应 `code === "-4"` 时清空内存 sid 与 cookie sessionInfo（会话过期）；统一返回 `ExamApiResponse{code,message,body,file,user}`。

封装函数：`queryQuestionAnswer(examId, attemptId)`、`submitQuestionAnswer(answerData)`、`queryNewExamPaper(examId)`。

## 十二、关键时序参数汇总

| 常量 | 值 | 用途 |
|------|----|----|
| MAX_ATTEMPT_COUNT | 10 | DOM 轮询尝试上限 |
| CHECK_INTERVAL_MS | 500 | 通用轮询间隔 |
| VIDEO_SEARCH_INTERVAL_MS | 1000 | SCORM 视频查找间隔 |
| VIDEO_PLAY_RESUME_INTERVAL_MS | 5000 | 暂停自动恢复轮询 |
| PLAYER_INIT_DELAY_MS | 500 | 播放器初始化后重置进度延迟 |
| TASK_EXPIRE_MS | 10h | 学习地图课程任务过期 |
| AES_CONFIG | KEY 16B / IV 16B | 考试响应解密（CBC/PKCS7） |
| 每日任务等待超时 | 30min | waitForTaskComplete |
| 关闭信号轮询 | 3s / 5s | close 信号 / 视频完成信号轮询 |
| 知识类积分估算 | +0.6/次 | 每 5 次真实校准 |
